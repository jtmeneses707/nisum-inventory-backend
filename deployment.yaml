apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-backend-pod
  template:
    metadata:
      labels:
        app: inventory-backend-pod
    spec:
      containers:
        - name: inventory-backend-container
          image: nexus.mynisum.com:2376/ascend-kafka/inventory-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
      imagePullSecrets:
        - name: registry-creds

---
apiVersion: v1
kind: Service
metadata:
  name: inventory-backend-service
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: inventory-backend-pod